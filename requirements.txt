# Pinned dependency ranges to tested compatibility
pandas>=2.2,<3.0
numpy>=1.26,<2.0
pyarrow>=12.0,<13.0  # fast parquet
textdistance>=4.5,<5.0  # fuzzy matching
tqdm>=4.65,<5.0
pytest>=7.0,<8.0
pytest-cov>=4.0,<5.0
